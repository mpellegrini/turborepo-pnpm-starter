{
  "private": true,
  "scripts": {
    "preinstall": "npx -y only-allow pnpm",
    "build": "turbo run build"
  },
  "engines": {
    "npm": ">=8.0.0",
    "node": ">=16.0.0"
  },
  "packageManager": "pnpm@7.20.0",
  "devDependencies": {
    "turbo": "1.6.3"
  }
}
